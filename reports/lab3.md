## 实现的功能
1. 移植上一个实验实现的系统调用
2. 为TaskControlBlockInner 添加task_priority和stride
3. 实现设置任务优先级的系统调用
4. 将任务队列改为优先队列,为TaskControlBlock实现Ord.修补更改后需要调整的代码
5. 实现spawn,新建TaskControlBlock,将子进程添加到父进程的子进程队列,子进程中添加父进程的Arc.将子进程添加任务队列
## 简答
1. 因为使用u8导致溢出,所以比较p1就不是比p2小了.
2. 因为即使溢出也可以计算出正确的大小关系,如果STRIDE_MAX溢出,STRIDE_MAX – STRIDE_MIN计算也会溢出.只要保证溢出值不超过BigStride / 2就可以正常的比较大小  
3.      let d = self.0.wrapping_sub(other.0);
        let h = BIG_STRIDE >> 1;

        if diff == 0 {
            Some(Ordering::Equal)
        } else if d < h {
            Some(Ordering::Greater)
        } else {
            Some(Ordering::Less)
        } 
**荣誉准则**
1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 **以下各位** 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

> 无

2. 此外，我也参考了 **以下资料** ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

>《rCore-Tutorial-Guide-2025S 文档》《RISC-V特权模式文档》

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。
   我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。
   我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。
   我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。
   我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。